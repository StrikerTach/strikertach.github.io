#include <studio.h>
int main()
{
int count;

    scanf("%d", &count);
        if (count == 10){
    printf("Hello, world");
    }
        else {
    printf("Goodbye, world!");
    }
        return 0;
}